import React from 'React';

export default () => {

  const styles = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    fontSize: '50px',
    color: '#ccc'
  }

  return(
    <h1 style={styles}>Ping!</h1>
  );
};